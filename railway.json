
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "echo 'Starting app...' && echo \"PORT=$PORT\" && echo \"HOST=$HOST\" && echo \"NODE_ENV=$NODE_ENV\" && node --version && ls -la dist/server/ && node dist/server/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
